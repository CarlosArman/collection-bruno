info:
  name: List user without filter
  type: http
  seq: 1

http:
  method: GET
  url: "{{urlBaseServerRest}}/usuarios"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
