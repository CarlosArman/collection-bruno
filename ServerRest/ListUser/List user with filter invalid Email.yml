info:
  name: List user with filter invalid Email
  type: http
  seq: 1

http:
  method: GET
  url: "{{urlBaseServerRest}}/usuarios?email=212121dssdsd"
  params:
    - name: email
      value: 212121dssdsd
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 400", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
