info:
  name: Get products category list
  type: http
  seq: 7

http:
  method: GET
  url: "{{urlBase}}/products/category-list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
