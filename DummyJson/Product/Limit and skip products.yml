info:
  name: Limit and skip products
  type: http
  seq: 4

http:
  method: GET
  url: "{{urlBase}}/products?limit=10&skip=10&select=title,price"
  params:
    - name: limit
      value: "10"
      type: query
    - name: skip
      value: "10"
      type: query
    - name: select
      value: title,price
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
