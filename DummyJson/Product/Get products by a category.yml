info:
  name: Get products by a category
  type: http
  seq: 8

http:
  method: GET
  url: "{{urlBase}}/products/category/smartphones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
