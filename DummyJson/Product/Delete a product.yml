info:
  name: Delete a product
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{urlBase}}/products/195"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
