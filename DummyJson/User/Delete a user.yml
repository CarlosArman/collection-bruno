info:
  name: Delete a user
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{urlBase}}/users/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
