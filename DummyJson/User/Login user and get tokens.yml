info:
  name: Login user and get tokens
  type: http
  seq: 8

http:
  method: POST
  url: "{{urlBase}}/user/login"
  body:
    type: json
    data: |-
      {
        "username": "emilys",
        "password": "emilyspass",
        "expiresInMins": 45
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
