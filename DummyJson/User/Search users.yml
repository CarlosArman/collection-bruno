info:
  name: Search users
  type: http
  seq: 3

http:
  method: GET
  url: "{{urlBase}}/users/search?q=John"
  params:
    - name: q
      value: John
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
