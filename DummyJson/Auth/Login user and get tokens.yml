info:
  name: Login user and get tokens
  type: http
  seq: 1

http:
  method: POST
  url: "{{urlBase}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "emilys",
        "password": "emilyspass",
        "expiresInMins": 45
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
