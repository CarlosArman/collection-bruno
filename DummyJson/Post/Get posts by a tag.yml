info:
  name: Get posts by a tag
  type: http
  seq: 7

http:
  method: GET
  url: "{{urlBase}}/posts/tag/life"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
